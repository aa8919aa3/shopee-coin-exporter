# 蝦幣儀表板計算與效能優化報告

日期：2026-08-14
發行版本：v1.1.1

## 結論

儀表板「期間淨變動」與蝦皮「目前可用蝦幣」不相等，主要不是加總公式本身錯誤，而是兩個數字的時間口徑不同。官方餘額是摘要 API 在抓取當下提供的帳戶快照；期間淨變動則只加總交易 API 目前仍可取得的歷史區間。交易 API 不提供該區間開始前的期初餘額，因此不能直接把期間淨變動當成目前餘額。

優化版已將兩者分開顯示，並在完整抓取後顯示以下對帳關係：

> 官方目前餘額 = 推估期初餘額 + API 可取得期間淨變動

2026-08-14 的實際 Edge 測試結果如下：官方目前餘額為 1,468.11，交易 API 可取得 2025-08-14 至 2026-08-14 共 10,954 筆紀錄，期間獲得 11,263.80、期間折抵／使用 10,336.00、期間過期 0.00，期間淨變動為 +927.80。因此推估期初餘額為 540.31，且 `540.31 + 927.80 = 1,468.11`，數字可以正確對帳。

## 原始落差的來源

| 原因 | 對數字的影響 | 本次處理方式 |
|---|---|---|
| 期初餘額不在交易 API 可取得區間內 | 期間淨變動少了區間開始前已存在的 540.31 | 明確改名為「期間淨變動」，並顯示推估期初餘額 |
| 官方餘額與交易流水是不同時間點與不同資料集 | 使用者新增或使用蝦幣時，兩者可能短暫不同步 | 摘要與交易結果保存各自抓取狀態，UI 清楚標示口徑 |
| 中途分頁失敗仍被舊程式當成成功 | 後段交易會靜默遺失，淨變動少算 | 改為 `complete`、`partial`、`stopped`、`failed` 四種結果 |
| 每 20 筆重新排序、重算與重繪全部資料 | 10,000 筆會產生近似平方級工作量並增加中斷機率 | 進度只更新筆數，完整排序、統計與圖表只在結束時執行一次 |
| JavaScript 浮點數直接累加 | 大量小數交易可能出現尾差 | 以 1/100000 蝦幣為最小單位使用整數加總 |
| 過期交易只影響淨值但未完整出現在圖表與分項 | 摘要與圖表無法互相核對 | 新增期間過期卡片，月圖與分類圖一致納入過期 |

## 已完成的程式優化

### 抓取生命週期與錯誤處理

`api.js` 現在使用單一 active run、run ID 與 `AbortController` 管理抓取。重新啟動會使舊工作失效，停止按鈕會真正取消正在等待的網路請求，`clear()` 後舊請求也不會把資料寫回。每個請求有 15 秒逾時，對 HTTP 408、429 與 5xx 採最多三次指數退避重試；401／403、非 JSON、schema 異常、重複頁面、空頁與 `has_more` 矛盾等狀況會分類回報。

抓取結果改成結構化狀態。只有明確抵達 API 尾端且沒有拒絕異常紀錄時才標示完整；中途錯誤、單筆髒資料或安全上限會保留已取得資料，但標示為部分結果。UI 不再把部分資料宣稱為「所有歷史紀錄」。

### 計算正確性

所有交易金額在進入資料模型時轉成最小單位整數，獲得、折抵、過期與期間淨變動都以整數完成加總，只在顯示或匯出邊界轉回小數。交易方向優先依金額正負判斷，避免正數退款或文字中包含「使用」時被錯誤反轉。

統計模型新增過期分項、有效／無效紀錄數與實際日期最小值／最大值。報表期間不再假設陣列第一筆與最後一筆必然是最新與最早，而是從有效 timestamp 計算。

### 效能

實際 API 支援每頁 100 筆，優化版已由每頁 20 筆提高至 100 筆。10,954 筆資料約由 548 次請求降為約 110 次請求。進度 callback 不再複製與排序整個 Map，也不再在每一頁重算統計、重建 SVG、全量篩選與重畫表格。

關鍵字搜尋加入 200ms debounce，表格僅建立目前頁面的 15 至 100 列。CSV 與 TXT 使用不可變快照、批次字串區塊與定期讓出事件迴圈，降低大型字串的主執行緒停頓與峰值記憶體。

### 安全性與健全性

表格不再把 API 或 DOM 文字拼進 `innerHTML`，而是使用 `textContent` 與 `DocumentFragment`，封堵交易標題、訂單編號等欄位造成的 DOM XSS。圖表動態文字會做 XML escaping，月份使用固定格式白名單。

CSV 的文字欄位會中和以 `= + - @` 開頭的試算表公式，金額欄則驗證為有限數字並輸出純數值。TXT 會移除控制字元並把換行轉成可見符號，避免內容偽造報表結構。CSS 的通用 class 已限制在擴充功能容器內，避免污染蝦皮原頁面。

## 驗證結果

| 驗證項目 | 結果 |
|---|---|
| 所有 JavaScript 語法檢查 | 通過 |
| Manifest JSON 與 Git whitespace 檢查 | 通過 |
| 精確小數加總與過期計算 | 通過 |
| 完整／部分／停止狀態測試 | 通過 |
| `clear()` 後舊工作不回寫 | 通過 |
| CSV 公式注入測試 | 通過 |
| 10,000 筆統計效能 | 2.7ms（本次 Mac 實測） |
| Edge 真實資料完整抓取 | 10,954 筆 |
| Edge CSV 匯出 | 1,223,454 bytes，共 10,955 行（含表頭） |
| Edge TXT 匯出 | 1,718,493 bytes，共 43,905 行 |

## 尚存風險與後續建議

蝦皮使用的是未公開的站內 API，端點、欄位或分頁規則未來仍可能改變。現在的 schema 驗證會把變更明確回報為部分或失敗，而不會靜默輸出錯誤完整資料，但仍需在蝦皮改版時更新解析器。

交易端點仍採 offset 分頁。若帳戶在抓取期間大量新增交易，理論上可能造成分頁邊界位移。此次將每頁提高到 100 筆後，完整抓取時間與風險已大幅降低；若未來 API 提供穩定 cursor，應優先改用 cursor。DOM fallback 只能取得當前頁面可見資料，因此永遠標示為部分資料，不應用來宣稱完整歷史。

目前 10,954 筆的統計與匯出效能已足夠，不需要立即引入 Web Worker。若未來資料量超過十萬筆或報表文字大幅增加，可再把序列化移到 Worker 或使用串流檔案寫入。

## 執行測試

```bash
node tests.js
```

本次優化納入 v1.1.1 發行版本。
